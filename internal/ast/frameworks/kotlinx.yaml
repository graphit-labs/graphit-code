# Kotlinx serialization and Gson detection rules.
# Extracted from frameworkRules (lines 280-282).

framework: kotlinx
languages: [kotlin]

decorator_detection:
  # Kotlinx serialization
  - name: Serializable
    category: serialization

  # Gson (commonly used alongside Kotlin)
  - name: SerializedName
    framework: gson
    category: serialization
