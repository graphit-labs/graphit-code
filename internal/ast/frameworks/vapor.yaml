# Vapor framework detection rules.
# Extracted from frameworkRules (line 475) — RouteCollection is a decorator in the Go source,
# but it's mapped as heritage here to match the user's schema request.

framework: vapor
languages: [swift]

# RouteCollection appears in frameworkRules as a decorator (line 475)
decorator_detection:
  - name: RouteCollection
    category: web

# Also specified as heritage per the user schema
heritage_detection:
  - parent: RouteCollection
    category: web
