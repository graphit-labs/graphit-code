language: cpp
grammar: tree-sitter-cpp
extensions: [".cpp", ".hpp", ".cc", ".cxx", ".hxx", ".hh"]
queries:
  # === Entity queries ===
  - data_key: functions
    graph_label: Function
    pattern: '(function_definition declarator: (function_declarator declarator: (identifier) @name))'
  - data_key: functions
    graph_label: Function
    pattern: '(function_definition declarator: (function_declarator declarator: (qualified_identifier name: (identifier) @name)))'
  - data_key: classes
    graph_label: Class
    pattern: '(class_specifier name: (type_identifier) @name)'
  - data_key: structs
    graph_label: Struct
    pattern: '(struct_specifier name: (type_identifier) @name)'
  - data_key: enums
    graph_label: Enum
    pattern: '(enum_specifier name: (type_identifier) @name)'
  - data_key: namespaces
    graph_label: Namespace
    pattern: '(namespace_definition name: [ (namespace_identifier) (nested_namespace_specifier) ] @name)'
  - data_key: types
    graph_label: Type
    pattern: '(type_definition declarator: (type_identifier) @name)'
  - data_key: imports
    graph_label: Module
    pattern: '(preproc_include path: (string_literal) @name)'
  - data_key: imports
    graph_label: Module
    pattern: '(preproc_include path: (system_lib_string) @name)'
  - data_key: parameters
    graph_label: Parameter
    pattern: '(parameter_declaration declarator: (identifier) @name)'
  - data_key: fields
    graph_label: Field
    pattern: '(field_declaration declarator: (field_identifier) @name)'

  # === Relation queries ===
  - data_key: calls
    type: relation
    relation_type: CALLS
    pattern: '(call_expression function: (identifier) @name)'
  - data_key: calls
    type: relation
    relation_type: CALLS
    pattern: '(call_expression function: (field_expression field: (field_identifier) @name))'
  - data_key: instantiations
    type: relation
    relation_type: INSTANTIATES
    pattern: '(new_expression type: (type_identifier) @name)'
  - data_key: heritage
    type: relation
    relation_type: INHERITS
    pattern: '(base_class_clause (type_identifier) @name)'
  - data_key: field_reads
    type: relation
    relation_type: READS_FIELD
    pattern: '(field_expression field: (field_identifier) @name)'

# C++ export detection: non-static functions are public
exports:
  strategy: no_static

# C++ uses 'this' for receiver type resolution
self_keywords: ["this."]

# Parent context type mapping — C++ tree-sitter node types only
# C++ uses class_specifier (not class_declaration), struct_specifier, namespace_definition, function_definition
context_types:
  class_specifier: Class
  struct_specifier: Struct
  namespace_definition: Namespace
  enum_specifier: Enum
  function_definition: Function

# C++ has no anonymous function expressions via variable_declarator
anon_func_types: []

# Declaration node types for docstring attachment — C++ tree-sitter only
declaration_types:
  - function_definition
  - class_specifier
  - struct_specifier
  - enum_specifier
  - namespace_definition
  - type_definition
  - declaration
  - template_declaration

# C++ tree-sitter uses a single "comment" node type for both // and /* */ comments
comment_types:
  - comment

# Entry point scoring rules for C++
entry_points:
  names:
    - pattern: main
      score: 80
    - pattern: "Test*"
      score: 60
    - pattern: "test_*"
      score: 60
    - pattern: init
      score: 40
    - pattern: setup
      score: 40
    - pattern: configure
      score: 40
    - pattern: "*Handler"
      score: 30
    - pattern: "*Controller"
      score: 30
    - pattern: "cmd*"
      score: 20
    - pattern: "run*"
      score: 20

  exported_bonus: 10
  max_score: 100

  # C++ has no decorators/annotations — this section is intentionally empty
  decorators: []
