It becomes invisible in `.js`/__INLINE_1 — and the cost has been measured, not estimated.

## O achado

`graphit ast index` reportava dois arquivos sob "Empty (0 entities)":
The two are just...
`export default { … }`.

Every pattern for `javascript.yaml` requires a NAME declaration - function or class.
variable, or import/export with string origin. An anonymous object default export
Brazilian Portuguese:
Literal does not declare any name, so nothing fit. The proof is the neighbor:

Idiomatic English:
The literal doesn't mention anyone's name, so it didn't match anything. The proof is the next door neighbor.
Here is the translation:

In _INLINE_0_, it did not enter the list because it uses
`import … from '…'`.

The comparison that turns this into a flaw rather than just a detail is: INLINE_0, in the same way.
directory, yields 20 + 20. The same configuration was achievable or not
Only because of the size of the file.

Three iterations, and what each one taught.

First - capture all pairs with their exact value. He resolved and cost +37% of us.
The sample showed the problem: half was actual content (the table)
The first half was a repeated call to `pkb → sql` and `plsql → sql` in the CodePanel.
site — `behavior: 'smooth'`, `block: 'center'`, `overflowX: 'auto'`.

Second - anchor on the exported object or a constant. Zero noise, cost + 7.8%.
The issue as reported was not resolved. In reality, the literal translations are three or four.
Levels below (`theme.extend.colors.border`), and the anchor only grabs the direct child. The
`tailwind.config.js` continuou vazio.

O teste que eu tinha escrito passava porque o fixture era achatado — a forma que eu
I imagined, not the one that exists. It was testing against the real file that got it. The test.
The `TestTheConfigFilesThatWereEmptyAreNotEmptyAnyMore` exists so that this does not happen again:
He reads from disk.__Inline 0__

"Tree-sitter" does not have a descendant cure or structural negation, so "any pair"
Literal, but not within quotation marks "is not expressible." The choice between the two was made by

The literal phrase is often used to emphasize that something cannot be expressed in a certain way or format. In this case, it's suggesting that an option isn't available for selection due to specific limitations or restrictions.

English: Literal, but not within quotation marks "is not expressible." The choice between the two was made by

This translation maintains the original meaning while making it more idiomatic in English.
Engineer, with both measurements on the table: **no anchor**.

Third - The structural key is also content. **INLINE_0** continued.
In zero, and the Engineer pointed out: even with just empty objects, the graph should know.
What does `autoprefixer` exist? Yes, it names an `plugins: { autoprefixer: {} }`.
plugin, and `{}` means "without options," not "nothing here." Capture the key (without value,
because there is no literal) cost +2.3 points (37.0% → 39.3%). Array item — glob items
Out: **came free**, just like the `json.yaml` already did.

## Resultado

| arquivo | antes | depois |
|---|---|---|
| `postcss.config.js` | 0 entidades | 3 (`plugins`, `tailwindcss`, `autoprefixer`) |
| `tailwind.config.js` | 0 entidades | 123 |

Cost: +39.3% of us in `.tsx`, +35.7% in `.ts`. Only these languages—here are 52
Of 703 files, then the global effect is small. In a purely front-end project would be
Material, and it's worth fixing there.

It became a dependency.

Achado no mesmo caminho: `require('tailwindcss-animate')` chegava como chamada a um stub
Called INLINE_0, with an unattainable name of the module — then "of this project"
It depends, lost the whole CommonJS now. It's an INLINE 0, via predicate INLINE 1 fixing it.
Callee with the auxiliary capture `@_fn` that does not become an entity.

The containment guard demanded twice, and both times he had reason.

Declare `context_types` in `javascript.yaml` and `tsx.yaml` — which had none of that...
He passed the test by checking all their containers, and revealed that
`function_declaration`, `method_definition` e afins nunca foram contextos ali: os
Parameters were assigned based on what was around them or discarded. Corrected accordingly.

**INLINE_0** remained as an exemption FOR THE REASON: unlike **INLINE_1**, which captures the entire pair.
And therefore declares INLINE_0 as context, here only the pair of literal values is captured, and
A captured pair never contains another. The one who possesses them is the closest named declaration.
— por isso `lexical_declaration` É contexto: `const X = { … }` possui os seus pares.

## Armadilha que eu repeti

Inserting `context_name_paths:` immediately after the first line of `context_types:` swallows it.
The remaining entries go inside it — YAML is valid, and the entries turned into.
Key → label on a map that expects key → path. I had already registered this in memory when...
Editing those blocks by modifying PostgreSQL/TSQL and falling again in TypeScript now.
script, reescreva o bloco inteiro.

## Progress Log

August 16, 2026 — Key/Value pair for configuration object and lookup table, value key
  estrutural, item de array, e `require()` como Import, em `javascript`, `typescript` e
  `tsx`. `shardCacheVersion` 6 → 7. Seis testes, um deles contra os arquivos reais.
Complete suite green, `make lint` 0 issues.
