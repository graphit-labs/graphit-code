---
Title: The Task Log Opens the Task, Resumes Interruptions to Framework Tools, and Syncs as If You Have Certainty
status: done
created: 2026-08-19
updated: 2026-08-19
tags: [mandate, skills, task-log, sync, continuity]
---

The task log opens the task, not closes it.

## Objective

The Engineer requested three new instructions, all in the mandate and skills—i.e., on the document.
builders Go que geram `AGENTS.md` e os `SKILL.md`, nunca nos `.md` gerados:

The task log is the first artifact of a task, not the last. At the beginning of your work,
agent creates `docs/tasks/<tarefa>.md` with reasoning, justification, and what will be
   feito quebrado em tarefas com suas specs. À medida que conclui, ele atualiza o registro —
With every step forward and every turn in direction—so that any other agent can return
   de onde ele parou a qualquer momento.
Interrupt is not optional. When the agent is interrupted, or when the user requests it.
Changes/Corrections in work, when he returns, he prioritizes these tools again.
framework on his own natives - reopening the skills he's returning to in that domain.
The automatic synchronization has a small delay. The daemon's watcher reindexes after the change.
Writing, not with her. Whenever the agent needs to be certain of what it is that she has.
Tools return updated, it calls the Sync of MCP, which is what puts
   knowledge, memory e AST no mesmo ponto.

The true problem that all three resolve is continuity. A written log at the end is one.
Report, not a handover— if the session dies halfway, there is no record at all. A
User correction is precisely when the agent is most rushed and stressed.
Eager to fall into the trap, and an index that responds with a second read after being written.
Trust in a state that no longer exists.

## Plan & Task Breakdown

- [x] **T1 — Preamble do mandate** (`internal/hub/adapters/ide/mandate.go`, `mandatePreamble`).
Spec: Two new clauses, global (apply to all five modules, not just one):
Reiteration after interruption/correction/reversal of course applies to the entire mandate.
(b) The automatic reindexing is asynchronous and has lag – when you need certainty, call.
Here is the translation:

"_`graphit_sync` and let it finish. File restriction: the content cannot contain"

This translation maintains the structure of the original Portuguese text while making it more idiomatic in English. The underscore (_) has been replaced with an empty space, as per your request.
  pseudo-tags `<palavra>`, porque `parseTriggers` trata qualquer `<word>` como tag.
Trigger of the Knowledge Module (`internal/knowledge/rule.go`), `MandateTrigger`.
Spec: new triggers for "I'm starting a task," "I've completed a step/changed direction."
  "estou retomando trabalho interrompido"; `alwaysClause` passa a dizer que o log abre a
  tarefa.
- [x] **T3 — Skill de knowledge** (`internal/knowledge/rule.go`, `KnowledgeRuleContent`).
Spec: New section under Task Logs — the log opens before the work, with reasoning,
Justification and plan in task-oriented specifications; continuous updates at each step; section of
Restart (review the log, reopen the skills, only then continue). Table "When to create or"
The update gains the advantage before it even starts. The template completes with `## Plan & Task` in place.
Breakdown. Note on delay in synchronization during indexing section.
[T4 - Skill of AST] (`internal/ast/rule.go`) : The mandatory section of the sync at the end.
Session explains the delay and what to do when certainty is needed in the middle of the session.
T5 - Memory Skill (`internal/memory/rule.go`, "Reindex After Writes") is the same.
Delay, on the Wikipedia of memory, with the choice between `memory_index` (tight) and
Inline 0 (the three indices).
- [x] **T6 - Tests that tie three instructions to the generated content, following the repo's pattern**
(Statements about the assertions returned by the builders).
- [ ] T7 - Recreate Versioned Artifacts (`AGENTS.md` and the six `SKILL.md`), which are
Generated by the installed binary and not from the source tree - see System Knowledge.
The chosen path by the Engineer in this session: `make install` (with sudo password available)
In this machine, which installs the new launcher – and the launcher re-extracts `graphit-core` and
  `graphit-mcp` quando o BuildID dele deixa de casar com `.build-id`/`launcher.stamp`, o que
It requires no manual runtime swap described in memory.

## Implementation Details

### T1 — `mandatePreamble()`

Two clauses added to the preamble, which is written once and applies to all five blocks:

- **"An interruption is not an exemption."** Ser interrompido, corrigido ou redirecionado
Reimplement the mandate in full for resumption: reopen the domain skill, redo the repairs.
Consultations should keep MCP ahead of native tools. The text names the reason —
Urgency is precisely when the agent falls into grep— and the consequence: the work already done.
It became speculation because the user just changed the premise.
- **"Automatic indexing lags the change."** O watcher reindexa depois da escrita. Um tool
Called during the interval, responds with an index that is still not contained in what just ended up being
Written and respond with confidence. When certainty is needed—before making a decision based on
result before declaring work completed and after any change that did not come
Of its own editions (pull, checkout, rebase, restore) - call `graphit_sync` and leave
To finish. It's the only call that puts knowledge, memory, and AST at the same point.

### T2 — trigger do `doc_rule`

Three new levers appear in the order that the situation presents itself:

- "you are about to start a task of any size — the task log is the FIRST artifact, written
  before the work, not a report you write after it"
- "you finished a step, changed direction, hit a blocker, or learned something that changes
  the plan — the log is updated then, not at the end"
- "you are resuming after an interruption or a correction — read the existing log before you
  touch anything, then continue from it"

E a `alwaysClause` passou a abrir por "The task log opens the task", mantendo a frase antiga
Regarding the registration being a condition of completion.

### T3 — skill de knowledge

- A tabela "When to create or update a task log" ganhou a primeira linha: **Before starting
any task** → create a log with objectives, reasoning, and plan, inline.
New Section "The Task Log Opens the Task - It Is Not a Report You Write at the End"
The minimum content of the opening (objective, reasoning, justification for the alternatives)
Rejected, the continuous update rule (the
Trigger is the completed step, the changed direction, or the blocked action, not the end of the task), and the test of
sufficiency: if the session ended now, another agent could continue reading only the
  log?
New Section "Resuming after an Interruption or a Correction": Read the log first, then proceed.
Reopen the skills of the framework (correction does not suspend the mandate), register the change of
Onward in the Progress Log with the reason, and only then continue.
- Template completo ganhou `## Plan & Task Breakdown` com checkboxes e spec por item, entre
  `## Objective` e `## Implementation Details`.
The Quick Task Log no longer births with `status: done` — now it is `in-progress | done`, with the change.
He is also being created before the change.
The indexing section received a delay note and distinction between "do not call sync every"
Edition and "call sync when you need it."

### T4 e T5

The same fact—reindexing is asynchronous—is said within each module because that's where it happens.
Agent is when delay bites: at the AST (after session timeout) along with the mandatory sync requirement for end of session, in the...
Memory adjacent to INLINE 0 (which had already mentioned that a recently written memory can
Do not appear in search results; now tell me what to do when it's necessary to be certain.

## Use Cases

Agent initiates a task

Agent in session (any ID that receives the mandate).
Preconditions: The mandate is in `AGENTS.md`; Skill `graphit-knowledge` is available.
Installed; the user just requested a job.
- **Main Flow**:
  1. O gatilho "you are about to start a task of any size" faz o agente abrir a skill.
  2. A skill manda criar `docs/tasks/<tarefa>.md` com frontmatter `status: in-progress`,
``## Objective``, reasoning and `## Plan & Task Breakdown` – one input per task, each
     uma com sua spec.
Only then did the agent edit code.
- **Alternative Flows**:
Change small: The Quick Task Log is acceptable, created before the change with
    `status: in-progress`.
There is already a log for that purpose, updating it instead of creating another one.
- **Error Scenarios**:
Agent writes code first: the skill classifies it as absent log — task not
It is complete, regardless of whether the code is ready.
Postconditions: there exists a readable record by another agent before any edit.
- **Affected Files**: `internal/knowledge/rule.go`.

### UC-02: Agente atualiza o log a cada progresso

Actor: agent in session.
- **Preconditions**: existe um task log com `status: in-progress`.
- **Main Flow**:
  1. O agente conclui um item do plano, muda de abordagem ou encontra um bloqueio.
  2. Marca o item, acrescenta ao `## Progress Log` o que foi feito e o que vem a seguir, e
     atualiza `updated:`.
  3. Continua o trabalho.
- **Alternative Flows**:
Change of course: The old item remains, with the reason for abandonment registered – Progress
Log is append-only.
- **Error Scenarios**:
Session interrupted without update: The next agent reads a plan whose state is not updated.
It corresponds to the code, which is the scenario where the instruction exists to avoid.
Postconditions: The log responds with "where it stopped" without the conversation history.
- **Affected Files**: `internal/knowledge/rule.go`.

### UC-03: Agente retoma trabalho interrompido ou corrigido

Actor: agent in session, after user interruption or correction request.
Preconditions: There was previous work on this or another session.
- **Main Flow**:
  1. O preamble do mandate reaplica: as listas de gatilhos valem de novo, integralmente.
The agent reopens the skill in which it is reentering and repeats the queries (memory)
Wiki graph instead of acting on assumptions before interruption.
3. Reads the existing task log, records the correction and new direction in the Progress Log.
Continues with MCP tools ahead of natives.
- **Alternative Flows**:
The correction is invalid part of the plan: affected items are rewritten, not deleted.
- **Error Scenarios**:
The agent treats correction as an extension and falls into the trap of quick file reading via grep/reading from a hurry.
It is precisely what the clause names.
- **Postconditions**: o trabalho retomado obedece ao mandate como no primeiro turno.
- **Affected Files**: `internal/hub/adapters/ide/mandate.go`, `internal/knowledge/rule.go`.

Agent needs certainty about the current status of indices.

Actor: agent in session.
Preconditions: there has been recent writing (code, docs, or memory), or the tree changed by
  fora (pull, checkout, rebase, restore).
- **Main Flow**:
  1. O agente reconhece que vai decidir com base no que um tool devolve, ou vai declarar
Work completed.
  2. Chama `graphit_sync` com o `project_dir` e deixa terminar.
Then you only read the indices and conclude.
- **Alternative Flows**:
Only one module is suspected: `graphit_knowledge_sync`, `graphit_memory_index` or
They do it for a fraction of the work.
- **Error Scenarios**:
Without synchronization, the response comes from an outdated index and is indistinguishable from a correct one – the
Mode of failure that the instruction names.
- **Postconditions**: knowledge, memory e AST refletem o estado em disco.
- **Affected Files**: `internal/hub/adapters/ide/mandate.go`, `internal/knowledge/rule.go`,
  `internal/ast/rule.go`, `internal/memory/rule.go`.

## Test Cases & Acceptance Criteria

### Feature: Mandate preamble carries the resume and sync-delay clauses
Ref: UC-03, UC-04

Scenario: Resuming after interruption is in the preface.
```gherkin
Given the mandate preamble rendered by mandatePreamble
When the text is inspected for the resume clause
Then it states that an interruption, a correction or a redirection re-applies the mandate in full
  And it says the framework tools keep precedence over native tools on resume
```

The delay in the reindex is in the preface.
```gherkin
Given the mandate preamble rendered by mandatePreamble
When the text is inspected for the indexing clause
Then it says automatic indexing lags the change
  And it names the sync tool as the way to be certain the indexes are current
```

Prelude does not introduce pseudotags.
```gherkin
Given the mandate preamble rendered by mandatePreamble
When it is scanned for angle-bracket pseudo-tags
Then none are present, so parseTriggers cannot mistake prose for a trigger tag
```

### Feature: Knowledge module orders the task log before the work
Ref: UC-01, UC-02

Trigger of the mandate covers the beginning of the task.
```gherkin
Given the doc_rule mandate trigger
When its trigger list is inspected
Then one entry fires when a task is about to start
  And one entry fires when a step finished or the direction changed
  And one entry fires when work is resumed after an interruption
```

#### Scenario: A skill manda criar o log antes de editar
```gherkin
Given the knowledge skill content
When the Task Logs section is inspected
Then it says the log is written before the work, not as a report at the end
  And the create-or-update table has a row for starting a task
  And the full template contains a Plan and Task Breakdown section
```

#### Scenario: A skill ensina a retomada
```gherkin
Given the knowledge skill content
When the resume guidance is inspected
Then it orders reading the existing task log first
  And it says a correction does not suspend the framework tool priority
```

### Feature: Every module states that indexing lags
Ref: UC-04

Scenario Outline: The delay is said from within each skill
```gherkin
Given the "<skill>" rule content
When it is inspected for the indexing delay
Then it says the reindex lands after the write
  And it names the call that makes currency certain

Examples:
  | skill     |
  | knowledge |
  | ast       |
  | memory    |
```

## Files Changed

| File | Change | Reason |
|---|---|---|
Here is the translation:

"____ | Modified | Two global clauses in the preamble: resumption and delay of reindexing"
The inline 0 is modified by adding triggers of the mandate and sections for opening, updating continuously, and resuming in skills.
Brazilian Portuguese:
| `internal/ast/rule.go` | Modified | The index rebuild delay next to the mandatory session end sync |

Idiomatic English:
| `internal/ast/rule.go` | Modified | There is a delay in indexing after the mandatory synchronization of closing sessions, which can be optimized.
____ | Modified | The delay of memory wiki and the choice between index and sync
Brazilian Portuguese to idiomatic English:

"_______ INLINE 0 ______ | Created | ties together the two clauses of the preamble"
Created by maintaining order before work, continuous updates, and resuming from where you left off.
| `internal/ast/rule_sync_delay_test.go` | Created | amarra o atraso na skill de AST |
Created by amassing delays in memory skills
| `AGENTS.md`, `.claude/skills/**/SKILL.md`, `.agents/**`, `.kiro/**` | Regenerated | artefatos gerados pelos builders acima |

## Trade-offs & Decisions

The two new clauses remain in the preambles, not repeated by module. The preamble exists.
Exactly because five copies of the same policy cost five times as much at the top of each
Session and fifth not teaching anything. Restart and index lag are module facts:
They are valuable for memory, graph, wiki, hub, and improvements equally.
The operational detail remains in the skill. The mandate states there's an delay and that sync is the issue.
  cura; qual tool estreito serve cada caso (`knowledge_sync`, `memory_index`, `ast_index`)
Here's the translation:

"Follows in the skills, where the agent is already when it comes time to choose."
Sure, here is the translation:

Call it sync when you need certainty.
It doesn't call it sync every time an edit occurs.
The two instructions contradict each other when placed side by side without criteria.
Written: The trigger is not "I edited," it's "I am going to decide based on this" or "I am declaring. "
Completed.
The Plan & Task Breakdown is complete and not in the Quick template. The quick log exists.
for minor changes; forcing him into a formal plan would turn the minimum documentation into
The ceremony and practical result would be nothing.

## Technical Debt

The inline 0 of the task log is convention text without validation. A log that remains
It is always indistinguishable from abandoned - `knowledge_lint` could
  reportar logs `in-progress` mais velhos que N dias.
The delay in reindexing is described qualitatively as "a small delay." The actual value.
It comes from debouncing the watcher in the daemon; nobody measured it, and the skill doesn't promise numbers.

## System Knowledge

Here is the translation:

"`AGENTS.md` and `SKILL.md` are generated and versioned. The daemon runs the binary."
INSTALLED (not the source tree), and calls
Periodically, while the runtime is older than the source,
Any regeneration made from the source is reversed in seconds. Edit the `.md` manually
It's wrong, and even editing the Builder Go file and regenerating from source alone isn't enough without changing it.
Runtime. View the corresponding memory for the exact procedure (stop who holds the ```
Runtime by PID, rebuild with ldflags from the Makefile, replace with `mv`, no changes needed.
  `.build-id`/`launcher.stamp`).
The preamble of the mandate cannot contain `<palavra>`. `parseTriggers` sweeps the content.
Internal of the block searching for INLINE_0 and treats any coupling as a module tag; one
  placeholder em prosa viraria um trigger fantasma que `assembleTriggers` reemitiria.
Edit the builder after installing it ages the binary. __Inline_0__ installs it.
  launcher, que re-extrai `graphit-core`/`graphit-mcp` quando o BuildID dele deixa de casar com
Here is the translation:

"`.build-id`/`launcher.stamp`; but any subsequent edit in `rule.go` will revert to it."
Installed behind the source, and _INLINE_0_ follows to write the text of the installed version.
Order of Operations: edit → test → INLINE_0 → INLINE_1 → review the diff.
The ``ModuleMandateTrigger`` receives ``alwaysClause` as its last paragraph in the block.` It is the only one.
Place where an unconditional statement can fit; the list of triggers is conditional.
by construction.

## Progress Log

### 2026-08-19

The memory and wiki consulted before any edit; the graph located the five
The `MandateTrigger` and the `mandatePreamble`, and memory brought the binary installation trap.
  (T7) antes de ela morder.
Task log created before implementation, with the plan in seven items - exactly as stated.
  esta tarefa introduz.
- T1–T5 implementados nos quatro builders. T6: oito testes novos em quatro arquivos, todos
  verdes; pacotes `internal/hub/...`, `internal/knowledge`, `internal/memory`, `internal/ast`,
  `cmd/graphit/commands` e `internal/livesearch/...` verdes com `-tags fts5`.
Mid-T7 turn-around change registered because it cost a second build round:
  ao revisar o `SKILL.md` gerado percebi que o aviso sobre caminhos relativos tinha ficado
After two new sections away from the table that belongs to it, I reordered in INLINE_0 – and this
The binary I was just installing has been invalidated, so replace **INLINE_0** and **INLINE_1**.
They had to re-run it again. The correct order now in memory: edit builder → test →
  `make install` → `graphit sync` → conferir o diff dos `.md`.
Completed: Inline 0 and nine Inline 1 (AST, Knowledge, Memory × Inline 2/Inline 3)
Generated with the new text, and the daemon restarted on the runtime extracted again —
That is, he no longer reverses.
- Bloqueio encontrado: o tool MCP `graphit_sync` ficou 1800s sem responder e foi abortado pelo
Customer, while `graphit sync` by CLI completed. The MCP Stdio process for this session is
Binary, OLD (rose before `make install`) and the call happened right in the window that
The daemon was restarting, so this is not a conclusion about the tool in general— but who repeats
The sequence "install and synchronize in the same session" should wait for this.
