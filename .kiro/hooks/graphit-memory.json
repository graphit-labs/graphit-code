{
  "hooks": [
    {
      "action": {
        "command": "'graphit' _session-hook --format plain-context",
        "type": "command"
      },
      "description": "Initialize Graphit memory before the first response.",
      "enabled": true,
      "name": "graphit-memory-session-start",
      "trigger": "SessionStart"
    },
    {
      "action": {
        "prompt": "Graphit invariant: reapply module routing before every new/resumed action. Read matching skills before first use; reuse per target/overrides, reload on their change; reload after compaction if lost. Cluster neighbors remain Graphit-managed: use their returned `dir` as `project_dir` for target MCP reads, not native discovery. Use Graphit MCP before native equivalents, with `ai_optimized: true` when supported. If a required tool is unavailable, use default native tools, never the Graphit CLI. Resume durable task state; new questions trigger needed enabled Memory/Task recall. Reuse sufficient context. `project_dir` is call-local; persist project identity and relative paths, never a machine-specific checkout root.",
        "type": "agent"
      },
      "description": "Reassert Graphit-first routing on every submitted or resumed turn.",
      "enabled": true,
      "name": "graphit-resume-routing",
      "trigger": "UserPromptSubmit"
    },
    {
      "action": {
        "command": "'graphit' _session-hook --format plain-unit",
        "type": "command"
      },
      "description": "Checkpoint task management after the smallest completed work unit.",
      "enabled": true,
      "name": "graphit-task-checkpoint",
      "trigger": "PostToolUse"
    },
    {
      "action": {
        "prompt": "Graphit task checkpoint: on a completed work unit of a claimed task, call `graphit_task_progress` now with evidence and next step. Reads and task bookkeeping alone are not completed units. Keep task state in Graphit. Before completion, verify acceptance checks and affected code/documentation consistency in both directions; record inspected targets and evidence in Task. Resolve divergence before closing.",
        "type": "agent"
      },
      "description": "Update task management as soon as a spec task completes.",
      "enabled": true,
      "name": "graphit-task-completed",
      "trigger": "PostTaskExec"
    },
    {
      "action": {
        "command": "'graphit' _session-hook --format no-output --sync",
        "type": "command"
      },
      "description": "Dispatch a complete Graphit sync asynchronously whenever the agent stops.",
      "enabled": true,
      "name": "graphit-final-sync",
      "trigger": "Stop"
    },
    {
      "action": {
        "command": "'graphit' _session-hook --format plain-context",
        "type": "command"
      },
      "description": "Initialize Graphit memory when the CLI agent starts.",
      "enabled": true,
      "name": "graphit-memory-session-start-cli",
      "trigger": "AgentSpawn"
    }
  ],
  "version": "v1"
}
