# The image consumes the Linux binary produced by the CI/local build. Everything else is excluded,
# keeping the build context small instead of shipping the checkout, coverage.out and .git.
#
# Ordering matters: the catch-all comes first, the exception after it.
*

!.build/
!.build/graphit-linux-amd64
